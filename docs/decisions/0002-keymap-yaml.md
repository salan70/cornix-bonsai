# keymap.yaml schema

Status: Proposed

## Context

`keymap.yaml` is intended to be the Git-managed desired state and should be readable by humans and AI agents while remaining convertible to and from the Semantic Model.

## Open decision

Define the schema after the Semantic Model decision is sufficiently stable.
